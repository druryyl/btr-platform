# IIS API application URL (includes /api path). Use the hostname clients use in the browser, not localhost.
# Override at build time if needed, e.g. $env:VITE_API_BASE_URL = "http://server:8080/api"
VITE_API_BASE_URL=http://server:8080/api
